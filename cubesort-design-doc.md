# Дизайн-документ: «Кубики» (рабочее название)

Головоломка-сортировка. Игрок раскладывает разноцветные кубики по столбикам так,
чтобы на каждом столбике оказались кубики одного цвета. За каждый собранный
столбик на фоне вырастает здание — постепенно строится деревянный макет города.

Платформы: Яндекс Игры (основная), Playgama Bridge (дистрибуция на 100+ порталов).
Ориентация: портрет. Управление: один палец. Сессия: 2–4 минуты.

---

## 1. Ядро геймплея

### Поле

- Внизу экрана — ряды **столбиков** (деревянный штырь на круглой подставке).
- На штырь надевается до `capacity` кубиков (базово 4).
- Кубиков всего `colors × capacity`. Столбиков всегда больше, чем цветов —
  разница даёт свободное место для манёвра.

### Ход

1. Тап по столбику → **верхняя группа** кубиков одного цвета отрывается и
   зависает над штырём (визуально приподнята, лёгкое покачивание).
2. Тап по другому столбику →
   - если он пуст **или** его верхний кубик того же цвета,
   - и там хватает свободных слотов на всю группу,
   - → группа перелетает по дуге и садится. Ход засчитан.
   - иначе → отказ: столбик коротко дёргается, звук «нет», группа остаётся в руке.
3. Повторный тап по исходному столбику → отмена, группа возвращается.

**Групповое перемещение обязательно.** Если сверху лежат 3 красных подряд —
они переезжают одним движением, а не тремя тапами. Без этого играть раздражает.

### Победа

Уровень пройден, когда каждый непустой столбик содержит кубики **одного цвета**
и заполнен полностью (`capacity` из `capacity`). Частично заполненные
одноцветные столбики победой не считаются — иначе уровень решается тривиально.

### Поражения нет

Тупик возможен, но проигрыша нет. Есть выходы:
- **Отменить** — откат последнего хода. Первые 3 бесплатно, дальше за rewarded-рекламу.
- **Подсказка** — подсвечивает пару «откуда → куда». 2 на уровень, дальше за рекламу.
- **+1 столбик** — добавляет временный пустой штырь. Один раз за уровень, за рекламу.
- **Заново** — сброс уровня, всегда бесплатно.

---

## 2. Крючок: город

Это то, что отличает игру от сотни одинаковых сортировок. Механика внутри
уровня знакомая — награда снаружи новая.

- Верхние ~40% экрана — **стол с макетом города**. Пустой на старте.
- Каждый **пройденный уровень** ставит на макет **домик** в цвет последнего
  собранного столбика. Отдельные собранные столбики внутри уровня приносят
  мелкие декорации — куст, дерево, фонарь — по кругу площадки.
  Награда не появляется на месте: она вылетает из собранной стопки
  и летит в город по дуге.
- Между уровнями макет **сохраняется и растёт**. Через 20 уровней это
  уже плотный квартал; появляются деревья, фонари, машинка, река.
- Каждые 10 уровней — новый **район** с новым силуэтом застройки
  (низкие домики → башни → мост → парк). Даёт ощущение прогресса без гринда.

Город хранится как простой массив построек в `localStorage` (плюс облако Яндекса,
если доступно). Он декоративный — не влияет на геймплей, не требует баланса,
не ломается. Дёшево в реализации, сильно по ощущению.

---

## 3. Кривая сложности

Три режима, в каждом 60 уровней и свой город. Прогресс режимов не
пересекается: игрок уходит и возвращается без потерь. Таблицы живут
в `src/game/modes.js`, здесь только их смысл.

### Лёгкий — деревянный посёлок

| Уровни | Цветов | Вместимость | Столбиков |
|--------|--------|-------------|-----------|
| 1–5    | 3      | 3           | цвета + 2 |
| 6–20   | 3–4    | 4           | цвета + 2 |
| 21–40  | 4–5    | 4           | цвета + 2 |
| 41–60  | 5      | 4           | цвета + 2 |

### Средний — каменный город

| Уровни | Цветов | Вместимость | Столбиков |
|--------|--------|-------------|-----------|
| 1–10   | 4      | 4           | цвета + 2 |
| 11–30  | 5      | 4           | цвета + 2 |
| 31–50  | 6      | 4           | цвета + 1 |
| 51–60  | 6      | 5           | цвета + 1 |

### Сложный — стекло и металл

| Уровни | Цветов | Вместимость | Столбиков |
|--------|--------|-------------|-----------|
| 1–10   | 5      | 5           | цвета + 2 |
| 11–30  | 6      | 5           | цвета + 1 |
| 31–50  | 7      | 5           | цвета + 1 |
| 51–60  | 7      | 5           | цвета + 1 |

Глубина перемешивания — базовая формула, умноженная на коэффициент
режима: 0.8 / 1.0 / 1.35. Бесплатных отмен, подсказок и запасных
столбиков на уровень: 5/3/1 в лёгком, 3/2/1 в среднем, 2/1/1 в сложном.

Первые три уровня лёгкого и среднего — обучение без единого слова текста:
уровень 1 решается за 2 хода. В сложном обучения нет.

Медаль даётся за пройденные все 60 уровней режима: золото от 165 звёзд,
серебро от 130, иначе бронза. Звёзды хранятся по уровням, сохраняется
лучший результат.

**Механика-твист — только в версии 1.1, не в MVP.** Кандидат: кубик двойной
высоты, занимающий два слота (не влезает в столбик, где остался один слот).
Второй кандидат попроще: кубик рубашкой вверх — цвет открывается, только когда
он становится верхним.

---

## 4. Генерация уровней

**Только обратная генерация. Никакого «случайно накидать и проверить решателем».**

```
1. Собрать решённое состояние: colors × capacity, каждый цвет на своём столбике.
2. Сделать M случайных ВАЛИДНЫХ ходов по обычным правилам игры.
   Предпочитать ходы, которые разбивают одноцветные группы
   (иначе перемешивание вырождается).
3. Если итоговое состояние = решённому — перегенерировать.
4. Записать состояние + seed в JSON.
```

Так уровень **гарантированно решаем**, и не нужен солвер в рантайме.
`M` = 30 + 4 × номер уровня, с потолком 200.

Для подсказки нужен поиск: BFS по состояниям с ограничением глубины 12 и
хешированием посещённых (состояние нормализуется — столбики сортируются,
чтобы одинаковые расстановки не считались разными). Если решение не найдено
за лимит — подсказка предлагает любой ход, ведущий к слиянию двух групп
одного цвета.

### Формат уровня

```json
{
  "id": 12,
  "capacity": 4,
  "colors": 5,
  "posts": [
    [0, 1, 0, 2],
    [3, 3, 1],
    [4, 0, 4, 1],
    [],
    []
  ],
  "parMoves": 14
}
```

Индекс в массиве `posts` — снизу вверх. Числа — индексы цветов из палитры.
Пустой массив — пустой столбик. `parMoves` — длина решения генератора
(для звёздочек: 3 звезды если ходов ≤ par, 2 если ≤ par×1.5, иначе 1).

Уровни 1–60 генерируются один раз офлайн скриптом и лежат в `levels.json`.
Дальше — бесконечная генерация на лету по номеру уровня.

---

## 5. Арт-направление

Все клоны этого жанра выглядят одинаково: тёмно-фиолетовый фон, глянцевые
неоновые фигуры, скруглённая жирная типографика. Идём в другую сторону.

**Направление: деревянный макет на столе при дневном свете.**
Крашеные детские кубики, тёплый песочный стол, мягкие длинные тени.
Спокойно, тактильно, «дорого» — и не похоже ни на кого в жанре.

### Палитра

| Роль | Hex |
|------|-----|
| Фон (верх) | `#F2EADA` |
| Фон (низ, поверхность стола) | `#DFD0B4` |
| Дерево штыря и подставки | `#9A7B52` |
| Тень | `rgba(92, 70, 44, 0.20)` |
| Текст и иконки | `#2E2A24` |

Кубики — приглушённо-насыщенные, как крашеное дерево, не неон:

| # | Цвет | Hex |
|---|------|-----|
| 0 | Красный | `#D4553F` |
| 1 | Жёлтый | `#E2A238` |
| 2 | Зелёный | `#5B9750` |
| 3 | Синий | `#3B71A4` |
| 4 | Фиолетовый | `#7A57A3` |
| 5 | Бирюзовый | `#3E9E93` |
| 6 | Розовый | `#C9628C` |

Кубики различаются не только цветом: на верхней грани каждого — **простой
вдавленный символ** (круг, квадрат, треугольник, крест, звезда, ромб, кольцо).
Это доступность для дальтоников и заодно деталь, добавляющая «сделанности».

### Типографика

- Дисплей (номер уровня, «Уровень пройден»): **Unbounded**, 700. Характерная
  геометрия, есть кириллица, не выглядит как дефолт.
- Основной текст и кнопки: **Onest**, 500/600. Спокойный, отлично читается
  мелким кеглем, кириллица родная.

Оба подключаются локальными woff2 — не с CDN. Яндекс Игры работают из архива,
внешние запросы к шрифтам ломают офлайн-загрузку.

### Рендер кубика

Изометрия 2:1. Кубик — три грани одним путём, без спрайтов:

```
верхняя грань:  осветлить базовый цвет на 18%
левая грань:    затемнить на 14%
правая грань:   затемнить на 34%
обводка:        затемнить на 50%, толщина 1.5 px
```

Плюс мягкая эллиптическая тень на подставке и общая тень столбика,
падающая вправо-вниз под 30°. Никаких градиентов и блика — плоские грани
плюс правильные тени читаются как дерево.

### Сигнатурный элемент

Растущий макет города. Вся смелость тратится сюда — остальной экран
дисциплинированно тихий. HUD минимальный: номер уровня сверху,
три кнопки снизу, больше ничего.

---

## 6. Ощущение (game feel)

Это то, что отличает «работает» от «достойно». Тайминги — не примерные,
их надо соблюдать.

| Событие | Длительность | Кривая |
|---------|--------------|--------|
| Подъём группы со штыря | 120 мс | easeOutQuad |
| Покачивание группы в руке | ±3 px, период 1.6 с | синус |
| Перелёт по дуге | 180 мс + 25 мс на каждый кубик сверх первого | easeInOutQuad |
| Высота дуги | 70 px над верхней точкой обоих штырей | — |
| Посадка | 90 мс | easeOutBack |
| Squash при посадке | scaleY 0.88 → 1.00 за 110 мс | easeOutBack |
| Отказ (нельзя положить) | дрожь 3 цикла, ±5 px, 220 мс | — |
| Столбик собран: волна подпрыгивания | 55 мс на кубик, подъём на 6% высоты кубика | easeOutBack |
| Вспышка-кольцо от основания | +180 мс, 260 мс, радиус 0.4 → 1.8 ширины кубика, alpha 0.5 → 0 | — |
| Крышка падает на стопку | +220 мс, 200 мс с высоты 1.2 кубика | easeOutBack |
| Squash крышки при посадке | scaleY 0.86 → 1.00 за 110 мс | easeOutBack |
| Щепки и тряска камеры | +240 мс, 8 частиц, живут 420 мс | — |
| Награда летит в город | +300 мс, 520 мс, дуга 40% расстояния, поворот до 25° и обратно | easeInOutQuad |
| Награда приземляется в городе | +820 мс, отскок 180 мс, волна по площадке 320 мс | easeOutBack |
| Переход на след. уровень | 550 мс | easeInOutCubic |

Дополнительно:
- При посадке — 6 частиц-щепок в цвет кубика, живут 400 мс, гравитация.
- При сборе столбика — вспышка-кольцо и **лёгкая тряска камеры**: 4 px, 180 мс, затухание.
- Тактильная отдача (`navigator.vibrate`): 8 мс на взятие, 12 мс на посадку,
  30 мс на сбор столбика. Отключается в настройках.
- `prefers-reduced-motion` — сокращает все длительности до 40% и убирает тряску.

### Звук

Синтез через WebAudio, без файлов — короткие деревянные щелчки
(шум + быстрое затухание) плюс тон.

- Взятие: тон 440 Гц, 40 мс.
- Посадка: щелчок + тон. Ноты идут по пентатонике вверх при укладке подряд
  (C-D-E-G-A-C'), сбрасываются через 2 с бездействия. Это главный источник
  «приятности» — как в Tetris Effect.
- Сбор столбика: аккорд из трёх нот, 400 мс.
- Отказ: приглушённый низкий тон 140 Гц, 90 мс.

Громкость по умолчанию 0.5. Кнопка mute в настройках, состояние сохраняется.

---

## 7. Экраны

1. **Загрузка** — логотип, полоса прогресса. Обязательна для Яндекса
   (`LoadingAPI.ready()` вызывается по завершении).
2. **Главный экран** — макет города во весь экран, крупная кнопка «Играть»,
   мелко: уровень N, настройки.
3. **Игра** — HUD сверху (уровень, кнопка «Заново», настройки),
   поле, кнопки снизу (Отменить, Подсказка, +Столбик).
4. **Уровень пройден** — оверлей: звёзды, «Далее», внизу — что добавилось в город.
5. **Настройки** — звук, вибрация, «Сбросить прогресс».

Никаких модалок с текстом на старте. Игрок попадает в первый уровень
из главного экрана одним тапом.

---

## 8. Монетизация

| Место | Тип | Правило |
|-------|-----|---------|
| Между уровнями | Interstitial | Не чаще, чем раз в 3 минуты и не раньше 3-го уровня |
| Отмена (после 3-й) | Rewarded | — |
| Подсказка (после 2-й) | Rewarded | — |
| +1 столбик | Rewarded | Один раз на уровень |
| Sticky-баннер | Баннер | Только на главном экране, не в игре |

Реклама **никогда** не показывается сразу после проигрыша или в момент,
когда игрок ждёт анимацию. Перед показом — обязательная пауза игры,
приглушение звука, после — возобновление.

---

## 9. Требования площадок

Полный список — в `yandex-games-publishing-guide.md`. Критичное:

- SDK Яндекса подключается до старта игры, `LoadingAPI.ready()` вызывается
  ровно один раз.
- Отключить контекстное меню, выделение текста, `-webkit-touch-callout`,
  двойной тап для зума, свайп-навигацию.
- Игра должна корректно работать без авторизации.
- Пауза и mute при `game_api_pause` / потере фокуса вкладки.
- Русский язык — единственный. Не заводить локализацию:
  именно она была причиной ранних отказов.
- Никаких внешних запросов: шрифты, звуки, всё локально.
- Вес архива минимальный — при чистом Canvas это единицы сотен килобайт.

Playgama Bridge подключается через тот же фасад `platform/sdk.js`.
**До интеграции — подтвердить в поддержке Playgama доступность выплат.**

---

## 10. Порядок работы (треки для Orca)

Треки A и B независимы и могут идти параллельно в разных worktree.
C зависит от A. D и E — в конце, последовательно.

**A. Логика** (сильная модель)
Чистые модули без Canvas и DOM: состояние, правила ходов, проверка победы,
обратный генератор, BFS-подсказка, undo-стек. Покрыто прогонами в Node.
Готово, когда 1000 сгенерированных уровней решаются генератором обратно.

**B. Рендер и арт** (сильная модель)
Изометрия, отрисовка кубика/штыря/подставки/теней, сцена, макет города,
адаптация под разные экраны. Работает на моковых данных, не ждёт трек A.

**C. Анимации и ощущение** (сильная модель)
Твин-движок, все тайминги из раздела 6, частицы, тряска, звук, вибрация.
Начинается после стыковки A и B.

**D. Платформа** (слабая модель)
Фасад SDK, две реализации, обёртки рекламы, сохранения, чек-лист Яндекса,
манифест, сборка архива.

**E. Рутина** (слабая модель)
Иконки, скриншоты для карточки, описание игры, README, генерация
`levels.json` скриптом, локальный woff2-сабсет шрифтов.

**Финал: плейтест на реальном телефоне.** Не на эмуляторе. Три человека,
которые видят игру впервые, без единого объяснения. Если кто-то не понял
правила за 15 секунд — проблема в игре, а не в игроке.

---

## 11. Границы MVP

В первой версии **есть**: 60 уровней, город, все анимации, звук, реклама,
сохранения, Яндекс SDK.

В первой версии **нет**: механики-твиста, скинов кубиков, ежедневных заданий,
лидербордов, магазина, второго режима. Всё это — после публикации,
если игра покажет удержание.
